import { createContext } from "react";

export const loginUserContext = createContext<string | null>(null);
export const Alertcontext = createContext<string | null>(null);
export const CartContext = createContext<any | null>(null);